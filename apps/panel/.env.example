# Supabase — claves públicas del front (anon key, NO la service_role)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL del server Node. En dev se usa el proxy de Vite, dejar vacío.
# En producción, si el server sirve el front en el mismo origen, también vacío.
VITE_API_URL=
